import 'package:flutter/material.dart';

class BorderRadiusConst {
  static final normal = BorderRadius.circular(20);
  static final low = BorderRadius.circular(15);
  static final veryLow = BorderRadius.circular(10);
}
